h_1	EQU	$4	; pointer to relocation table
h_2	EQU	$6	; pointer to library linking table 1 (count & names)
h_3	EQU	$8	; pointer to library linking table 2 (handles)
h_4	EQU	$A	; relocation count
h_5	EQU	$C	; program comment / library name
h_6	EQU	$E	; program entry pointer / library routine table
